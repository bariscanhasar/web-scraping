{
  "scripts": {
    "start": "NODE_PATH=./dist node dist/server.js",
    "build": "tsc -p .",
    "dev": "nodemon -L arabam.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cheerio": "^1.0.0-rc.12",
    "nodemon": "^2.0.22",
    "pg": "^8.10.0",
    "puppeteer": "^20.1.0",
    "request": "^2.88.2"
  }
}
